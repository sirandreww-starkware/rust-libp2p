syntax = "proto3";

package turbine.pb;

message Shred {
    uint64 slot = 1;
    uint32 index = 2;
    uint32 shred_type = 3; // 0 = Data, 1 = Code
    bytes data = 4;
    bytes signature = 5;
    bool last_in_slot = 6;
}

message RPC {
    oneof message {
        Shred shred = 1;
    }
}
